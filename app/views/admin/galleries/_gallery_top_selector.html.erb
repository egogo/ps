<ul class="menu">
	<% items.each_index do |i| %>
		<li <%= 'class="first"' if i == 0 -%>>
			<%= link_to items[i].ru_title, admin_gallery_path(items[i]), :class => ( (params[:id].to_s == items[i].id.to_s) ? 'active' : '' ) %>
		</li>
	<% end %>
</ul>